# you can reference files in other python files if they are in the same folder
# but the file can't have spaces, and shouldn't start with a number.

def multiply2Numbers(firstNum, secondNum): # it includes parameters
    productNum = firstNum * secondNum # it does something with the parameters
    return productNum # give the result of the multiplication back to wherever you call the function
